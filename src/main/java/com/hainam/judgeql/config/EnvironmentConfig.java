package com.hainam.judgeql.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for environment variables
 * Note: The actual loading of environment variables is done in the main application class
 */
@Configuration
public class EnvironmentConfig {
    // Environment configuration is handled by DotenvLoader in the main application
}
